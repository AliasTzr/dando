import 'package:flutter/cupertino.dart';

class VideoReader extends StatelessWidget {
  const VideoReader({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}