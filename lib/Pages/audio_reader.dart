import 'package:flutter/cupertino.dart';

class AudioReader extends StatelessWidget {
  const AudioReader({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}